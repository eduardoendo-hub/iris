name: Cron · Ingest GA4 / Meta / Google

# Roda a cada 15min pra puxar dados das fontes externas pro IRIS.
# Idempotente — re-rodar no mesmo bucket sobrescreve via upsert.
#
# Secret necessario no repo (Settings → Secrets and variables → Actions):
#   IRIS_CRON_SECRET = mesmo valor do CRON_SECRET no Coolify do iris
on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: # permite rodar manualmente em Actions tab

jobs:
  ga4:
    name: GA4 ingest (claude-pro)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: POST /api/cron/ga4
        run: |
          curl --fail-with-body --silent --show-error \
            -X POST "https://iris.technowhub.ai/api/cron/ga4?product=claude-pro&days=7" \
            -H "X-Cron-Secret: ${{ secrets.IRIS_CRON_SECRET }}" \
            -H "User-Agent: github-actions-cron-iris" \
            -w "\n[http_code: %{http_code}, time: %{time_total}s]\n"

  # Stubs pra Fase 3/4 — descomentar quando os endpoints existirem
  # meta:
  #   name: Meta Ads ingest
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - run: |
  #         curl --fail-with-body -X POST \
  #           "https://iris.technowhub.ai/api/cron/meta?product=claude-pro" \
  #           -H "X-Cron-Secret: ${{ secrets.IRIS_CRON_SECRET }}"
  # google:
  #   name: Google Ads ingest
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - run: |
  #         curl --fail-with-body -X POST \
  #           "https://iris.technowhub.ai/api/cron/google?product=claude-pro" \
  #           -H "X-Cron-Secret: ${{ secrets.IRIS_CRON_SECRET }}"
